{
    "builds": [{
        "src": "titanicproject/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "titanicproject/wsgi.py"
        }
    ]
}